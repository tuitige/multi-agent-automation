{
  "name": "infra",
  "version": "0.1.0",
  "description": "AWS CDK infrastructure for multi-agent automation",
  "main": "lib/app.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "npm run build && cdk deploy --all",
    "diff": "npm run build && cdk diff",
    "synth": "npm run build && cdk synth",
    "destroy": "cdk destroy --all"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "20.1.0",
    "jest": "^29.4.0",
    "ts-jest": "^29.1.0",
    "aws-cdk": "2.100.0",
    "typescript": "~5.1.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.100.0",
    "constructs": "^10.0.0"
  }
}
